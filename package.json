{
  "name": "degate_protocols",
  "version": "0.1.0",
  "description": "degate monorepo",
  "main": "index.js",
  "repository": "https://github.com/degatedev/protocols.git",
  "author": "DeGate Fundation",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "wsrun": "wsrun",
    "build": "lerna link && wsrun build $PKG --fast-exit -r --stages --exclude-missing"
  },
  "devDependencies": {
    "lerna": "^3.16.0",
    "wsrun": "^5.0.2",
    "husky": "^4.0.4"
  }
}
